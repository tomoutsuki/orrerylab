import Link from "next/link";
import { OrbitalSystem } from "@/components/orbital/OrbitalSystem";
import { artists } from "@/content/artists";
import { works } from "@/content/works";
export const metadata = { title: "Artists" };
export default function ArtistsPage() { return <><section className="section page-hero" style={{position:"relative"}}><div style={{position:"relative",zIndex:1}}><span className="eyebrow">Artist trajectories / 02</span><h1 className="display">Distinct identities. One creative universe.</h1><p className="lede">Two directions, linked by an attention to emotional centre, clear form and lasting resonance.</p></div><div className="orbital-wrap" style={{opacity:.4}}><OrbitalSystem compact/></div></section><section className="section"><div className="artist-split">{artists.map((artist) => <article className={`artist-panel ${artist.visualMode === "cinematic" ? "cinematic" : ""}`} key={artist.slug}><span className="eyebrow">{artist.role}</span><h2>{artist.name}</h2><p className="lede">{artist.statement}</p><p className="quiet">{artist.description}</p><p className="quiet">Selected study: {works.find((work) => work.artist === artist.name)?.title ?? "To be introduced"}</p><Link className="button secondary" href={`/artists/${artist.slug}`} style={{marginTop:"auto",alignSelf:"flex-start"}}>Follow this trajectory</Link></article>)}</div></section></>; }
