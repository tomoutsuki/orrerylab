import { WorkArchive } from "@/components/works/WorkArchive";
import { works } from "@/content/works";
export const metadata = { title: "Works" };
export default function WorksPage() { return <><section className="section page-hero"><span className="eyebrow">Archive / 01</span><h1 className="display">Works that continue after the moment.</h1><p className="lede">An evolving archive of composition, experiments and worlds in formation. Launch entries marked as placeholders are ready for final material.</p></section><section className="section"><span className="eyebrow">Archive view</span><WorkArchive works={works}/><hr className="rule" style={{marginTop:"6rem"}}/><div style={{marginTop:"4rem"}}><span className="eyebrow">Timeline view</span><div className="timeline">{[...works].sort((a,b) => b.year-a.year).map((work) => <article className="timeline-item" key={work.slug}><span className="metadata">{work.year} · {work.artist}</span><h2>{work.title}</h2><p className="quiet">{work.emotionalCentre}</p></article>)}</div></div></section></>; }
